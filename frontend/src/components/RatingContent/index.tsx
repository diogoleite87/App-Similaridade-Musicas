import { Container } from "@material-ui/core"
import ListRadingVerified from "../ListRatingVerified"

export default function RatingContent() {


    return (
        <Container maxWidth="lg">
            <ListRadingVerified />
        </Container>
    )
}